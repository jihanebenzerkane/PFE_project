<?php require 'vendor/autoload.php'; $app = require_once 'bootstrap/app.php'; $kernel = $app->make(Illuminate\Contracts\Console\Kernel::class); $kernel->bootstrap(); $v = new App\Services\VerificationService(); $anomalies = $v->checkPlannings(); echo 'Found ' . count($anomalies) . ' anomalies.' . PHP_EOL; foreach(array_slice($anomalies, 0, 5) as $a) { echo $a['message'] . PHP_EOL; } 
