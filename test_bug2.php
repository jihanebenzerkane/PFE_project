<?php require 'vendor/autoload.php'; $app = require_once 'bootstrap/app.php'; $kernel = $app->make(Illuminate\Contracts\Console\Kernel::class); $kernel->bootstrap(); $snap = App\Models\PlanningSnapshot::latest()->first(); if(!$snap) { echo 'no snap'; exit; } foreach($snap->data as $row) { if(count($row['examinateurs'] ?? []) < 2) { echo 'Incomplete jury for P' . $row['projet_id'] . PHP_EOL; } } echo 'Done.'; 
